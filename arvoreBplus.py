class arvoreBplus:


    def no (self,ordem):
        self.efolha = False
        self.chave 
        self.tamanho = [None]*ordem,0
        self.ponteiro = [None]*(ordem+1)

    def __init__(self):
        self.raiz = None
    
    def inserir(self,elemento):
        if(self.raiz == None):
            self.raiz = arvoreB_mais.no 
            self.raiz.chave[0] = elemento
            self.raiz.efolha = True
            

    def buscar(self,chave):
        if self.raiz == None:
            print('Árvore vazia!')
        else:
            aux = self.raiz

            while (not aux.efolha):
                for i in range (aux.size):
                    if chave:



    def remover(self):
        print()

    def mostrar(self):

